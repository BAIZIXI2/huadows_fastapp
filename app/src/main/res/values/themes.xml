<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. -->
  <style name="Base.AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Customize your theme here. -->
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        
    <item name="colorPrimary">@color/accent_orange</item>
 
    <item name="colorSecondary">@color/accent_orange</item>
    
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowActionBar">false</item>
    <item name="android:windowFullscreen">true</item>
    <item name="android:windowContentOverlay">@null</item>
    
    <item name="android:windowBackground">@color/black</item>

  </style>

  <style name="AppTheme" parent="Base.AppTheme" />
  
  
  
  
  
  

    <style name="AppManageButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center_vertical</item> <!-- 使图标和文字垂直居中 -->
        <item name="android:paddingStart">16dp</item>
        <item name="android:drawablePadding">16dp</item> <!-- 图标和文字之间的间距 -->
        <item name="android:textAllCaps">false</item> <!-- 不将文字大写 -->
        <item name="android:foreground">?android:attr/selectableItemBackground</item> <!-- 点击反馈效果 -->
    </style>
    
    
        <!-- 自定义底部弹窗主题 -->
    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
        <!-- 使弹窗背景透明，以便我们自己的drawable背景生效 -->
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <!-- 设置我们自定义的带圆角的背景 -->
        <item name="android:background">@drawable/dialog_background</item>
    </style>

    
    
    <!-- 新增：用于外部安装器 Activity 的透明主题 -->
    <style name="Theme.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
  